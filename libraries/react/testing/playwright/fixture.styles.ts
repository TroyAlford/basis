import { css, style } from '@basis/react'

style('playwright:dialog-fixture', css`
  body {
    font-family: system-ui, sans-serif;
    padding: 2rem;
  }

  output {
    display: block;
    margin-top: 1rem;
  }
`)
