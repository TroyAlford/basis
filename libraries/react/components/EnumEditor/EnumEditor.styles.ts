import { css, style } from '../../utilities/style'

style('basis:enum-editor', css`
  .enum-editor.component {
    // Base styles for enum editor
  }
  
  .enum-editor .value {
    // Styles for the selected value display
  }
`)
