export enum LineCap {
  /** No cap - line ends exactly at the end point */
  Butt = 'butt',
  /** Rounded cap that extends beyond the end of the line */
  Round = 'round',
  /** Square cap that extends beyond the end of the line */
  Square = 'square',
}
